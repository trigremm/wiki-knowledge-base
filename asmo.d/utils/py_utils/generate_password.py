# from uuid import uuid4; print("\n".join(f"{str(uuid4())}-{str(uuid4()).upper()}" for _ in range(20)))

from uuid import uuid4

print("\n".join(f"{str(uuid4())}-{str(uuid4()).upper()}" for _ in range(20)))
